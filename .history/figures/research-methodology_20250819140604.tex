\begin{tikzpicture}[
    node distance=2.8cm and 3.5cm, % row and column spacing
    box/.style={rectangle, draw, fill=blue!10, text width=3.2cm, text centered, minimum height=1.5cm, rounded corners=3pt},
    phase/.style={rectangle, draw, fill=gitopsgreen!20, text width=4.2cm, text centered, minimum height=2cm, rounded corners=5pt, thick},
    arrow/.style={->, thick, gitopsblue}
]

% Title
\node[above] at (0,6.5) {\Large \textbf{Two-Phase Research Methodology}};

% Phase 1
\node[phase] (phase1) at (-5,4) {
    \textbf{Phase 1}\\
    \textbf{Single-Service Analysis}\\
    \small August 2-3, 2025\\
    \small 20 test scenarios
};

% Phase 1 components (spread horizontally)
\node[box, below left=of phase1] (p1baseline) {Baseline\\Establishment};
\node[box, below=of phase1] (p1control) {Controlled\\Comparison};
\node[box, below right=of phase1] (p1failure) {Failure\\Testing};

% Phase 2
\node[phase] (phase2) at (5,4) {
    \textbf{Phase 2}\\
    \textbf{Multi-Service Analysis}\\
    \small August 15-16, 2025\\
    \small 47 experiments
};

% Phase 2 components (spread horizontally)
\node[box, below left=of phase2] (p2complex) {Complexity\\Normalization};
\node[box, below=of phase2] (p2multi) {Multi-Service\\Testing};
\node[box, below right=of phase2] (p2hybrid) {Hybrid\\Integration};

% Results
\node[phase, fill=gitorange!20, below=3.5cm of p1control, text width=6cm] (results) {
    \textbf{Statistical Analysis}\\
    \textbf{\& Conclusions}\\
    \small p < 0.01 significance\\
    \small 316,481 bytes data
};

% Arrows between phases
\draw[arrow] (phase1) -- (phase2) node[midway, above] {\small Progression};

% Arrows from Phase 1 to components
\draw[arrow] (phase1) -- (p1baseline);
\draw[arrow] (phase1) -- (p1control);
\draw[arrow] (phase1) -- (p1failure);

% Arrows from Phase 2 to components
\draw[arrow] (phase2) -- (p2complex);
\draw[arrow] (phase2) -- (p2multi);
\draw[arrow] (phase2) -- (p2hybrid);

% Arrows to results
\draw[arrow] (p1control) -- (results);
\draw[arrow] (p2multi) -- (results);

% Key findings boxes (spread horizontally)
\node[box, fill=gitopsgreen!30, text width=2.8cm, below left=5cm and -1cm of results] (finding1) {
    \small \textbf{GitOps:}\\
    100\% automation\\
    37s recovery
};

\node[box, fill=gitred!30, text width=2.8cm, right=of finding1] (finding2) {
    \small \textbf{Traditional:}\\
    2.3x faster builds\\
    Manual gates
};

\node[box, fill=gitorange!30, text width=2.8cm, right=of finding2] (finding3) {
    \small \textbf{Hybrid:}\\
    Zero overhead\\
    integration
};

\node[box, fill=gitopsblue!30, text width=2.8cm, right=of finding3] (finding4) {
    \small \textbf{Auth Config:}\\
    65\% performance\\
    impact
};

% Arrows to findings
\draw[arrow] (results) -- (finding1);
\draw[arrow] (results) -- (finding2);
\draw[arrow] (results) -- (finding3);
\draw[arrow] (results) -- (finding4);

\end{tikzpicture}
